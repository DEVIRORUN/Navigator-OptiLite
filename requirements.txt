# Navigator-OptiLite — Python Dependencies
# PyTorch is installed separately via ROCm wheel (see setup.sh)
# pip install -r requirements.txt

ultralytics>=8.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
ipywidgets>=8.0.0
ipython>=8.0.0
jupyterlab>=4.0.0
notebook>=7.0.0
