# Navigator-OptiLite — Python Dependencies
# PyTorch installed separately via ROCm wheel in setup.sh
# Run: pip install -r requirements.txt

ultralytics>=8.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
ipywidgets>=8.1.0
jupyterlab-widgets>=3.0.0